# v2: Planner, Router, Sequencer, Spotter

v1 showed that Jev can run inside a real-arm control loop (138 ms median, no errors) and is good at
matching words to objects and places. It did not show that Jev beats a script: the task was
scriptable, and the script ended up in its prompt (`docs/assessment.md`). v2 gives each part only the
job it is good at.

## Principles

- **Jev chooses; code moves.** Jev picks among options code has already checked. Code owns
  geometry, arithmetic and limits.
- **The LLM stays off the critical path.** Apart from the first plan, the arm never waits
  mid-motion for an LLM: it carries on, or holds at a safe point.
- **Ask Jev only what code can't answer**, and show what gets worse without each question.
- **Safety never depends on a model.**

## Layers

| Layer | Runs on | When | Job |
|---|---|---|---|
| Perception | VLM + depth/tracking | VLM once per new object; tracking at frame rate | Produce the world state |
| Router | Jev | A correction, a skill failure, a change the Spotter can't place, or low confidence elsewhere | Decide who handles it (below) |
| Sequencer | Jev | A new plan arrives; a skill finishes or fails | Keep the plan on track: check the plan before it runs, check progress after each skill, adjust within the plan, or hand to the Router (below) |
| Spotter | Jev | Perception sees a change the robot didn't cause | Classify the change and pick an intervention (below). Overrides the Sequencer |
| Planner | LLM: a fast model, or a more capable one | New task, or when the Router hands off | Turn the task into a plan, or repair it |
| Harness | Code | Always | Hold the plan and world state; take events off the bus; call the right layer; apply answers; run skills through the safety filter |
| Skills | Code | While running | Execute; report done or failed |
| Safety filter | Code | Every command | Clip to workspace, speed and constraints |

## How it runs

Everything is event-driven. Perception, the user and skills put events on a bus; the harness takes
each one, calls the right layer asynchronously, and applies the answer when it arrives.

1. Perception and the harness start.
2. The user gives a task. With no plan yet, the harness asks the fast LLM for one (the one planned
   wait).
3. The Sequencer checks the plan (~150 ms); the first skill runs as soon as it passes.
4. From then on, changes, skill results and corrections arrive as events. The Jev and LLM calls they
   trigger run in parallel with the arm, and their answers come back as events too.

## Keeping the plan on track

The plan comes from a fast model and can be wrong, so a task can fail with nothing outside changing.
The Sequencer checks it twice, both times against the task (the user's words), not the plan:

- **Before it runs.** Narrow yes/no questions where yes means something is wrong: "Is `red_3`
  something the task asks about but the plan leaves out?", "Does step 2 move an object the task
  doesn't mention?". Any yes above threshold sends the plan to the capable LLM. This is TypeSafe's
  SDE cascade: cheap model, Jev check, expensive model only when the check fires.
- **After each skill.** One question per object ("Does the task want `red_3` in the left bin?"),
  and code compares the answers with where each object is and with the plan. Jev is never asked
  the multi-step "is the whole task on track?" as the deciding question: that is the indirection
  Jev is weak at. A holistic question can ride along as a signal, but doesn't decide.

From those, code picks: carry on, retry, skip, a different next step from the plan, or hand to the
Router when the plan itself looks wrong. Code checks what it can measure (the gripper is holding
something); Jev answers only the per-object questions of meaning.

When a skill fails, one Jev request carries both the Sequencer's and the Router's questions (they
are answered in parallel, so this adds no latency). If the Sequencer is confident and the Router says
continue, the Sequencer's pick runs: the fast case. Otherwise the Router's route wins: the case too
complex for the Sequencer. The same step failing twice always goes to the Router.

Two failure sources, two layers: the Sequencer catches problems the robot or the plan caused, the
Spotter catches changes from outside.

## Spotting changes

Perception flags any change the robot didn't cause: an object moved, appeared or vanished, or
something isn't where the plan expects. Code detects the change; the Spotter decides what it means
and picks one intervention:

| Change | Intervention |
|---|---|
| The block the arm is going for was moved | Re-target it |
| A sorted block was taken back out | Put it back on the to-do list |
| A hand reaches toward the arm | Pause, or back off |
| The hand withdraws | Resume |
| A block flickers in and out (sensor noise) | Ignore |
| A new object appears on the table | Hand to the Router |

The code safety filter keeps its own hard limits, including a stop distance around hands, whatever
the Spotter decides.

## Routing

The Router keeps execution moving across the layers. For each event it picks one route, in the same
request as any parameter changes:

| Route | Example | Cost |
|---|---|---|
| Continue | Chatter from the user | None |
| Adjust | "Actually, reverse it": set the sort order parameter | ~150 ms |
| Fast LLM | "Not that one"; a grasp failed twice | ~1 s |
| Capable LLM | A new task; the plan no longer fits the table | Seconds |
| Pause / ask the user | "Hang on"; a request it can't make sense of | Until the user answers |

Each route has its own confidence threshold, set by what a wrong answer costs: low for pause (it
is cautious), higher for adjust, highest for continue and resume. Below its threshold, a route
escalates. The same request asks a complexity score, which chooses between the fast and the capable
LLM. This is TypeSafe's intent routing and confidence-gated routing patterns.

Parameters are set the way TypeSafe's function-calling cookbook sets arguments: for each parameter,
one choice over its values, plus a yes/no "did the user say anything about this?". Code changes a
parameter only when that yes/no is high, so "unchanged" is never an option hidden inside the choice.

Escalating when it wasn't needed costs time; carrying on when it should have escalated can cost a
wrong action. The arm keeps running or holds at a safe point while
an LLM works; it never waits mid-motion.

## Terms

| Term | Meaning |
|---|---|
| World state | Objects (id, name, position, attributes) plus robot state (holding, current skill, last result) |
| Task | What the user asked for: the final arrangement. The order blocks are placed in is up to the plan unless the user says otherwise |
| Plan | An ordered list of skills with objects and parameters filled in |
| Parameters | Settings in the plan that a correction can change: sort order, pace |
| Change | Something in the world the robot didn't cause: an object moved, appeared or vanished |
| Intervention | What the Spotter tells the arm to do about a change |
| Constraint | A rule that holds for the whole task ("don't touch the green one"). It attaches to a tracked object, so it moves with it |
| Instruction / correction | Text from the user; a correction is one given mid-task |
| Route | Where the Router sends an event: continue, adjust, fast LLM, capable LLM, pause |
| Skill | A capability with preconditions and a done/failed result |
| Harness | The code at the centre: plan, world state, event bus, calls to Jev and the LLM, skills |
| Safety filter | Code that clips every command to limits and constraints |

## Skills

```
precondition(world_state) -> bool   # the Sequencer only sees skills that can run now
start(args)                         # objects and parameters from the plan
step()                              # a short chunk of motion; the safety filter clips it
status() -> running | done | failed(reason)
```

Skills are scripts for now. The interface is the same one a VLA policy would use, so a dexterous
skill can be added later without changing anything around it.

## Example: a mid-task correction

The task is "line blocks 1-6 up in the tray, lowest on the left". The plan has a `sort order`
parameter (lowest or highest on the left), and code computes each block's slot for either value. Blocks 1 and 2 are placed and the arm is carrying
3 when the user types "actually, reverse it".

| t | What happens |
|---|---|
| ~150 ms | Router: adjust, sort order = highest on the left, after this block |
| ~150 ms | Code switches the parameter and recomputes every slot. 3 goes to its new slot (4), then 4 to slot 3: the next blocks whose slots are free |
| 0-3 s, in parallel | Blocks 1 and 2 now sit in the slots 6 and 5 need, so the Router also hands that part to the fast LLM, which patches the plan to move them |

A correction that no parameter covers is routed to an LLM: the arm stops at a safe point and holds
while it replans.

This is the headline demo: the task changes in plain language mid-run, with no restart. Whether Jev
alone can handle "reverse it", or it needs the fast LLM, is what E11 tests.

## Writing Jev questions

From TypeSafe's jaggedness notes and cookbooks; the Planner's question templates follow these too.

- **Code does the arithmetic.** Counting, ranking, distances and comparisons are computed; Jev gets
  the result or a named band ("near", "in the arm's path").
- **One judgment per question, one hop.** Loop over objects in code and ask each one directly,
  rather than asking one question about all of them.
- **Say exactly what you mean.** Jev reads literally: state the condition, put boundary cases in
  the criteria, and keep instructions and criteria saying the same thing.
- **Checks: yes means something is wrong.**
- **Ask about the action wanted, not the subject.** "Is the user asking the robot to do something
  different?" separates a correction from chatter; "is this about the blocks?" does not.
- **No voting on choices.** Below a top-probability floor the answer is "uncertain" and goes to the
  Router. Averaging a few differently-worded yes/no questions is fine for a single gate.
- **Filter the state** to what the question needs; irrelevant detail costs accuracy.

## Rules that hold everywhere

- Priority: STOP button > safety filter > Spotter > Router > Sequencer > plan.
- STOP never depends on a model: the STOP button, and "stop" typed by the user, act in code. A model
  can only pause the arm; it resumes when the user says so, or the Spotter or Router decides to.
- Perception only flags changes the robot didn't cause. Code keeps a simple expectation of what the
  robot itself moves: the held object travels with the gripper, and objects within a few cm of a
  grasp may shift.
- Jev layers run on events, never on a clock, which keeps them well under the 20 requests/s account
  limit.

## Data capture

Every run records every Jev request and answer, every LLM call, the world state at each decision and
a camera frame at each event. A decision someone marks wrong gets its correct answer attached, and
the growing set is re-run on every prompt change. E12's logs are the first entries.

The dashboard shows each route, intervention and plan change as it happens, and is where a wrong
decision gets marked. Its design gets its own pass.

## Stages

- **Crawl:** sort blocks into bins or a tray on a static table, with several sorting rules and no code
  changes. Scripted skills; build in sim, accept on the real arm. Perception stays cheap: coloured,
  numbered blocks, an overhead camera with AprilTag calibration, colour plus depth for position, and
  a VLM only to read each block's number once.
- **Walk:** a person interferes: moves the target, takes a block back out, reaches into the path.
  Pass = the task still finishes, with no contact with the hand. Compared against the same system
  with the Spotter and Router replaced by hand-written rules, with some changes and corrections the
  rules can't anticipate.
- **Later:** VLA skills for objects a script can't handle, and a Planner that streams its plan in
  pieces so the first steps run while it works out the rest.

Diagrams of the loop and the main scenarios: `docs/v2-diagrams.md`.

## Spikes

- **E11** (`experiments/e11_reorder.py`): one decision after "reverse it". Can Jev work out the new
  order itself, or only map the correction onto code's answer?
- **E12** (`experiments/e12_blocksworld.py`): a text-only blocks world run closed-loop through the
  Sequencer, Spotter and a Listener (the Router's first version), with a scripted person and runs
  with each layer switched off. Its code still uses older terms (Listener, knobs, utterance, orders).

Both need a TypeSafe key to run; see `docs/handoff.md`.

Both predate the TypeSafe-docs pass above: their parameter questions hide "unchanged" inside the
choice, and E12's Sequencer asks one holistic question rather than one per object. Read their
parameter and Sequencer numbers as a lower bound for the current design.

## Open questions

- How many parameters a plan should expose.
- What the arm does while an LLM reviews a correction: carry on, or pause at a safe point.
- Router thresholds per route, and whether its escalation (handle here, fast LLM, capable LLM, ask
  the user) works better as one score whose levels are the routes, as in TypeSafe's entity
  alignment cookbook. The same question for the Spotter's interventions, which are ordered by
  caution.
- Skill failures: how often the Sequencer's fast pick is right, against the Router's route (E12).

## What the spikes say

E11 and E12 ran against `jev-1.13.0` on 2026-09-23 (`context/08-experiment-results.md`, E11 and E12).
E12 tests an earlier version of this design (a Listener instead of the Router, a Sequencer that picks
the next skill), so its conclusions about layers apply to that version.

- **The fast path can handle a correction without an LLM, if code solves every branch.** Jev read
  "actually, reverse it" and the other order changes perfectly: `intent` 719/720, `new_order`
  720/720. It could not work out the new action itself: in `raw`, "reverse it" while holding a block
  got the right direction 9/15 times and the right slot 8/15. With code's answer for both orders in the
  state, the action questions were 301/303. Closed-loop, E12 `solved` completed 40/40 runs with 0
  violations and a 2-3 tick correction. So the design's "Router: adjust, code recomputes every slot"
  row holds, and E11's headline answer is: Jev alone for mapping the words, code for the geometry, the
  fast LLM only for moving blocks already placed.
- **Code must precompute** anything that is a rank or a sequence position: each block's slot under
  every parameter value, the next block, the held block's destination, and *where in the per-block
  skill sequence the arm is*. Sparse block numbers halved Jev's slot accuracy (26/48 vs 53/55), and in
  24/103 answers Jev's `direction` contradicted its own `target_slot`. E12 `raw` failed 30/30 runs at
  the first `release`: Jev lifted the lowered block back up (`retreat`, 7,162 of 7,168 times) because
  nothing in the state said "lowered at the destination, not yet released". The better move is to ask
  Jev only the semantic question (`new_order`) and look the action up in code.
- **Confidence can drive the Router; a self-assessment question cannot.** Jev's `escalate` answer
  did not separate right from wrong requests (AUROC 0.43 and 0.36). The weakest confidence in the
  request did (0.93 with `solved` facts, 0.96 for E12 `raw` skill picks). At ≥0.5, `solved` answers were
  100% accurate with 97–100% of them clearing the gate. Thresholds must be per question: E12's flat
  0.3 let 99% of the wrong `retreat` picks through, and `when` had no gate at all, so a 0.06-confidence
  answer cost 7.5 s.
- **What E12 must change before it tests the current design:**
  1. Replace the Listener with the Router: routes (continue / adjust / fast LLM / capable LLM / pause),
     not intents, with a model able only to pause and "stop" handled in code.
  2. The Sequencer checks the plan before it runs and asks one question per object after each skill
     ("does the task want block 3 in slot 3?"), instead of reading the oracle's `next_step`; code
     compares the answers with the world and picks the skill.
  3. On a skill failure, send the Sequencer's and Router's questions in one request, and route the same
     failure twice to the Router.
  4. The Spotter handles any change the robot didn't cause (moved, appeared, vanished, a hand), and
     its instructions stop restating the oracle's thresholds, so it is no longer a lookup.
  5. Per-route thresholds applied to the weakest confidence in the request (including `when`), and
     parameters set with a "did the user say anything about this?" question instead of an
     `unchanged` option.
  6. Add a fact for the arm's position in the skill sequence, then rerun `raw`. As it stands, E12 `raw`
     never got past block 1 and never tested sorting or reversal.
  7. Add a loop detector: the same two skills alternating more than twice goes to the Router.

The "Writing Jev questions" section above already matches what the spikes found. The data adds a few
points: state the arm's place in the skill sequence literally, name slots by role rather than by
number, gate on confidence rather than a self-assessment question, and add `not_for` boundaries to
options that compete. These are listed, each with its number, in `context/08-experiment-results.md`.
