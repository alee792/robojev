[project]
name = "robojev-experiments"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx[http2]>=0.27", "numpy>=1.26"]

[project.optional-dependencies]
baseline = ["anthropic>=1.0"]
